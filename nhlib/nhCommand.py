class NhCommand :
    COMMANDS = ["$home", "$popular", "$search", "$newest",
                "$goto", "$next", "$open", "$watch", "$close"]
    
    # @formatter:off
    HOME    = "$home"
    POPULAR = "$popular"
    SEARCH  = "$search"
    NEWEST  = "$newest"
    GOTO    = "$goto"
    NEXT    = "$next"
    OPEN    = "$open"
    WATCH   = "$watch"
    CLOSE   = "$close"
    # @formatter:on